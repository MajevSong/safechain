# CouchDB state-database credentials for the local SafeChain dev network.
# These are LOCAL DEVELOPMENT defaults only — CouchDB is reachable solely
# inside the Docker network and never exposed to the public internet.
# Copy to ".env" and override before any non-local/shared deployment.
COUCHDB_USER=admin
COUCHDB_PASSWORD=adminpw
