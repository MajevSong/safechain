# Dependencies / installed SDKs
node_modules/
**/node_modules/

# Fabric binaries & samples (re-fetched via install-fabric.sh)
fabric-samples/
fabric-install.log
*.tar.gz

# Large generated benchmark logs (raw); keep the HTML report summaries
caliper/results/*.log

# Crypto material / generated network state (regenerated by scripts)
network/organizations/
network/channel-artifacts/

# Office binaries: keep paper.md (source); the .docx/.pdf are generated via pandoc
*.docx
*.pdf

# Large official datasets (public — see veriseti/extracted_real_figures.md for source + figures)
veriseti/*.xlsx

# Editor / OS
.DS_Store
Thumbs.db
*.tmp
_extracted.txt

# Coverage output
chaincode/coverage/
.nyc_output/

# Figure backups
figures/_orig/
